import React from "react";

const ScrollIcon = () => {
  return (
    <div className="icon-scroll mt-32 hidden 2xl:flex">
      <span className="icon-scroll__before"></span>
    </div>
  );
};

export default ScrollIcon;
